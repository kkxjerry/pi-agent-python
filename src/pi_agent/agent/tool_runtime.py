"""Tool execution implementation boundary.

Phase 8-12 keeps the executable batch runtime in :mod:`pi_agent.agent.agent_loop`
so lifecycle events and transcript ordering are reviewed in one place. This
module reserves the public boundary for a later extraction without maintaining
a second, divergent implementation.
"""
